import grafica.*;

void setup(){
  size(300,300);
  background(100);
  
  int nPoints=100;
  GPointsArray Points = new GPointsArray(nPoints);
  
  for (int i=0;i<50;i++){
    points.add(i,i);
  }
  
  for (int i=0;i<50;i++){
    points.add(i,50-i);
  }